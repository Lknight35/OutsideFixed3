# Outside App

Vercel-ready Vite + React + TypeScript project.

## Vercel settings
- Framework preset: Vite
- Root directory: repository root
- Build command: `npm run build`
- Output directory: `dist`

No package-lock.json is included intentionally so Vercel resolves the pinned public package versions cleanly.
